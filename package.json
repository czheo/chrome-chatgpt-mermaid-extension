{
  "name": "chrome-chatgpt-mermaid-extension",
  "private": true,
  "version": "0.1.0",
  "description": "Chrome extension to toggle Mermaid rendering in ChatGPT",
  "scripts": {
    "test": "node --test tests/*.test.mjs tests/unit/*.test.mjs tests/e2e/*.test.mjs"
  }
}
